load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "sandbox",
    crate_name = "codex_sandbox",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
    test_tags = ["no-sandbox"],
)
